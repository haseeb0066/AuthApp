# React Native Auth App

## 🌟 Features

- **User Authentication**
  - Sign Up / Login with email and password
  - Form validation for all fields
  - Error messages displayed below each input

- **Password Security**
  - Toggle to show/hide password
  - Minimum length validation

- **Persistent Login**
  - Login session saved using **AsyncStorage**
  - Users remain logged in even after app restart

- **Navigation**
  - Stack Navigator using **React Navigation**
  - Dynamic initial route based on first-time launch

- **UI & Styling**
  - Clean, modern, and colorful design
  - Reusable components like `CustomInput` and `CustomButton`
  - Responsive design for different device sizes

- **Bonus Tasks**
  - Secure password toggle
  - AsyncStorage for session persistence
  - Polished color theme with consistent branding

---

## 🛠 Tech Stack

- **React Native** & **Expo**
- **React Navigation** (Stack)
- **AsyncStorage** for persistent login
- **React Context API** for authentication state
- **@expo/vector-icons** for icons
- **Custom reusable components** for inputs & buttons
